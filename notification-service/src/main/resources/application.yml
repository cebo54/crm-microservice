spring:
  application:
   name: "notification-service"

server:
  port: 8084


eureka:
  client:
    service-url:
      defaultZone: "http://discovery-server:8761/eureka/"
    register-with-eureka: true
    fetch-registry: true